import React from 'react'
import HeaderBar from './HeaderBar';
import ConfigPanel from "./ConfigPanel";

const Root = () => (
    <div>
        <HeaderBar/>
        <ConfigPanel/>
    </div>
)

export default Root
